# 本地发布优先使用 npm login 的登录态。
# 不在项目级 .npmrc 写入 //registry.npmjs.org/:_authToken=${NPM_TOKEN}，
# 避免未设置 NPM_TOKEN 时覆盖本机登录态并导致 npm publish 认证失败。
#
# 如需使用镜像，可在用户级 .npmrc 中配置：
# registry=https://registry.npmmirror.com

后续可以使用 suit-skills web 直接启动一个本地服务  --- 已经完成

目前看起来还有一些问题：
1. 我希望默认安装的不是在项目下，默认下载的地址是当前用户名下的 .agents 然后使用软链接的方式到不同的平台
2. 后续可以直接部分在服务器上，然后支持包的上传与下载

然后与目前睿哥的平台可以直接打通，这样是最好的一种方式


此外还有一些细节的问题：
1. 比如一刷新，当前的tab就被消除，页面回到了 SKILLS上 --- 已经完成（使用URL hash持久化tab状态）
2. TAGS目前看起来没有什么作用 --- Tags页面点击tag后会跳转到Skills并自动过滤，这是设计意图
3. Installed 部分的搜索栏应该是固定的，不应该随下面的列表而滚动 --- 已经完成



现在还发现有一些问题，还有一些想法：
1. 目前的对于已经安装的部分的扫描结果可能没有包括软链接的部分
2. 需要添加几个默认的源，可以先暂时不开启
3. 能不能做成桌面应用
4. 启动时如何发现端口号被占用可以进行+1处理，直到不被占用


再后面的想法就是我也上传到github上去，然后主要在于如何宣传的问题，我如果进行有效的宣传的呢？
上传到github主要是为了可以进行自动化相关的编译，比如可以编译成mac